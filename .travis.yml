language: node_js
node_js:
  - '8'

cache:
  yarn: true
  directories:
    - '.eslintcache'
    - 'node_modules'

install:
  - yarn global add codecov
  - yarn
script:
  - yarn test --coverage
  - codecov
